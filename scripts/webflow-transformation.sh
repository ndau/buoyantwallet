#! /bin/bash

# You must already have extracted/exploded the zip file
# into an `.appconfig` folder within the top level of this
# project. This command uses data there and puts it into
# a build folder that gets created
npx appfairy